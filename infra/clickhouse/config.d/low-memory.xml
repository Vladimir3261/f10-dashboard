<clickhouse>
    <!-- Listen on all interfaces so the ingest container can reach CH
         over the compose network. CH ports are NOT published to the host
         (only `expose:`), so this stays private to the compose network. -->
    <listen_host>0.0.0.0</listen_host>

    <!-- Tuned for a small single-vehicle VPS (~2 GB RAM). Caps memory so
         ClickHouse cannot OOM the box during a backfill insert. -->
    <max_server_memory_usage_to_ram_ratio>0.6</max_server_memory_usage_to_ram_ratio>
    <mark_cache_size>268435456</mark_cache_size>
    <uncompressed_cache_size>0</uncompressed_cache_size>
    <max_concurrent_queries>16</max_concurrent_queries>
    <logger><level>warning</level></logger>
</clickhouse>
