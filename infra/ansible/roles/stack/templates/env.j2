# Rendered by Ansible (stack role) from your local environment - do not
# edit on the server; change infra/.env on your machine and re-deploy.
CH_USER={{ dotenv.get('CH_USER', 'default') }}
CH_PASS={{ dotenv.get('CH_PASS', '') }}
INGEST_TOKEN={{ dotenv.get('INGEST_TOKEN', '') }}
INGEST_BIND={{ dotenv.get('INGEST_BIND', '127.0.0.1') }}
GF_ADMIN_PASSWORD={{ dotenv.get('GF_ADMIN_PASSWORD', '') }}
GF_BIND={{ dotenv.get('GF_BIND', '127.0.0.1') }}
CH_MEM_LIMIT={{ dotenv.get('CH_MEM_LIMIT', '1400m') }}
