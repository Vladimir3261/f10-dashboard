# Rendered by Ansible (stack role) from your local environment - do not
# edit on the server; change infra/.env on your machine and re-deploy.
CH_USER={{ lookup('env', 'CH_USER') | default('default', true) }}
CH_PASS={{ lookup('env', 'CH_PASS') }}
INGEST_TOKEN={{ lookup('env', 'INGEST_TOKEN') }}
INGEST_BIND={{ lookup('env', 'INGEST_BIND') | default('127.0.0.1', true) }}
GF_ADMIN_PASSWORD={{ lookup('env', 'GF_ADMIN_PASSWORD') }}
GF_BIND={{ lookup('env', 'GF_BIND') | default('127.0.0.1', true) }}
CH_MEM_LIMIT={{ lookup('env', 'CH_MEM_LIMIT') | default('1400m', true) }}
